<view class="container">
  <!-- 用户信息 -->
  <view class="user-box" wx:if="{{detail}}">
    <image wx:if="{{currentUser.avatar}}" src="{{baseUrl + currentUser.avatar}}" class="avatar"></image>
    <view class="user-info">
      <text class="nickname">{{currentUser.nickname}}</text>
      <text class="create-time">{{formatTime(detail.createTime)}}</text>
    </view>
  </view>

  <!-- 求购信息卡片 -->
  <view class="goods-info">
    <!-- 标题+紧急标签+收藏爱心 -->
    <view class="title-wrap">
      <view class="goods-title">
        <text wx:if="{{detail.isUrgent === 1}}" style="color: #ff4444;">【急需】</text>
        {{detail.title}}
      </view>
      <!-- 收藏爱心 -->
      <view class="heart-btn {{isCollected ? 'collected' : ''}}" bindtap="toggleCollect">
        {{isCollected ? '♥' : '♡'}}
      </view>
    </view>

    <view class="attr-row">
      <text class="attr-label">类别</text>
      <text class="attr-text">{{detail.category}}</text>
    </view>
    <view class="attr-row">
      <text class="attr-label">预算</text>
      <text class="attr-text" style="color: #ff4444; font-weight: bold;">
        {{detail.price != null ? '¥' + detail.price : '面议'}}
      </text>
    </view>
    <view class="attr-row">
      <text class="attr-label">联系方式</text>
      <text class="attr-text">{{detail.contact}}</text>
      <button class="contact-btn" bindtap="showSendMsg">💬 私信</button>
    </view>

    <view class="desc-wrap">
      <view class="desc-title">需求描述</view>
      <view class="desc-content">{{detail.content}}</view>
    </view>
  </view>

  <!-- 评论区 -->
  <view class="comment-wrap">
    <view class="comment-title">全部评论</view>
    <view class="comment-item" wx:for="{{commentList}}" wx:key="index">
      <image class="avatar" src="{{item.avatar}}"></image>
      <view class="comment-right">
        <view class="name">{{item.nickname}}</view>
        <view class="content">{{item.content}}</view>
        <view class="time">{{formatTime(item.createTime)}}</view>
      </view>
    </view>
    <view class="empty" wx:if="{{commentList.length == 0}}">暂无评论，快来抢沙发~</view>
  </view>

  <!-- 评论输入框 -->
  <view class="comment-input-wrap">
    <input class="input" placeholder="说点什么..." bindinput="onInputComment" value="{{commentContent}}"/>
    <button class="submit-btn" bindtap="submitComment">发布</button>
  </view>
</view>

<!-- 私信弹窗 -->
<view wx:if="{{showMsgModal}}" class="modal-mask" catchtap="hideSendMsg">
  <view class="modal-card" catchtap="noop">
    <view class="modal-title">发私信给 {{currentUser.nickname}}</view>
    <textarea class="modal-input" placeholder="请输入私信内容..." value="{{msgContent}}" bindinput="onMsgInput" maxlength="200" auto-height></textarea>
    <view class="modal-actions">
      <button class="modal-cancel" bindtap="hideSendMsg">取消</button>
      <button class="modal-confirm" bindtap="doSendMsg">发送</button>
    </view>
  </view>
</view>